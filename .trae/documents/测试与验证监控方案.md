# 测试与验证方案

为了确保监控方案能够正常工作，我将执行以下步骤进行验证：

## 1. 脚本逻辑验证 (Manual Test)
- **进程检测验证**：手动运行 `monitor_isp.sh`，检查其是否能正确识别当前系统中 `Input Source Pro` 的内存占用情况。
- **模拟触发验证**：
  - 我将创建一个临时测试脚本 `test_monitor.sh`，将内存阈值设为极低（例如 1MB）。
  - 运行该脚本，验证其是否能正确进入“重启逻辑”（包括发送退出指令、检查进程状态以及重新拉起应用）。
- **日志验证**：检查 `~/Library/Logs/isp_monitor.log` 是否记录了正确的运行状态和重启信息。

## 2. 配置文件验证 (Configuration Check)
- **语法检查**：使用 `plutil -lint` 命令验证 `com.user.isp_monitor.plist` 文件的格式是否符合 macOS 标准。
- **路径检查**：确认 `.plist` 中的脚本路径与实际路径一致。

## 3. 结果反馈
- 我将展示测试运行的输出结果，并确认所有功能模块（检测、判断、重启、日志）均已准备就绪。

请确认是否开始执行测试？