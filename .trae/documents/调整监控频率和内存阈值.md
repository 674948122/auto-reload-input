# 修改配置方案

根据您的需求，我将调整现有的监控脚本和配置文件，具体修改内容如下：

## 1. 调整监控脚本 (`monitor_isp.sh`)
- **内存阈值**: 将 `THRESHOLD_MB` 从 `512` 修改为 `60`。
  - 备注：Input Source Pro 在正常使用下内存占用可能就会接近或超过 60MB，这个阈值设置得比较低，可能会导致频繁重启。如果您确认这是预期行为，我们将按此执行。

## 2. 调整自动化配置 (`com.user.isp_monitor.plist`)
- **检查频率**: 将 `StartInterval` 从 `600` (10分钟) 修改为 `3600` (1小时)。

## 3. 验证与更新
- 修改完成后，我们将更新 `README.md` 中的配置说明，确保文档与实际配置一致。
- 提醒您重新加载 `launchd` 配置文件以使新的频率设置生效。

请确认是否立即执行这些修改？